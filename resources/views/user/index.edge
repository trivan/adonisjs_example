<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Hello Adonis</title>
  {{ style('style') }}
</head>
<body>
  <section>
    <div class="subtitle">
      <p>Index page</p>
      <p><a href="{{ route('UserController.create') }}">Create new user</a></p>
    </div>

    <table>
      <tr><td>User name</td><td>Email</td><td>Created date</td></tr>
      @each(user in users)
        <tr>
          <td style="width:120px;"> {{ user.username }} </td>
          <td style="width:200px;"> {{ user.email }} </td>
          <td> {{ user.getCreatedDate() }} </td>
        </tr>
      @endeach
    </table>
  </section>
</body>
</html>
